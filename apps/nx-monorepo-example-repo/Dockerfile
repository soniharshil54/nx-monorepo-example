# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build nx-monorepo-example-repo`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t nx-monorepo-example-repo`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system nx-monorepo-example-repo && \
          adduser --system -G nx-monorepo-example-repo nx-monorepo-example-repo

COPY dist/apps/nx-monorepo-example-repo nx-monorepo-example-repo/
RUN chown -R nx-monorepo-example-repo:nx-monorepo-example-repo .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix nx-monorepo-example-repo --omit=dev -f install

CMD [ "node", "nx-monorepo-example-repo" ]
